apiVersion: v1
kind: Service
metadata:
  name: coherence-fund-api
  namespace: coherence-fund
spec:
  selector:
    app: coherence-fund-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8010

