"""Release pipeline helpers (prompt 67).

Public submodules:
  * ``changelog`` — conventional-commit parser + Markdown renderer.
  * ``cut_release`` — release driver (semver bump + changelog + readiness gate).
"""
