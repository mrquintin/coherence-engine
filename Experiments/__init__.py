"""Replication / re-analysis harnesses (prompt 47, Wave 13+).

Each subpackage owns one independent study with a frozen pre-registration
and a deterministic CLI entrypoint. See the README in each subdirectory.
"""
